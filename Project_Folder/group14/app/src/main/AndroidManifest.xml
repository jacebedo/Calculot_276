<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="calculotprototype.g14.cmpt276.calculot_prototype">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".AddUser" />
        <activity android:name=".Login" />
        <activity android:name=".ConfirmPassword" />
        <activity android:name=".calcGame.GameActivity" />
        <activity android:name=".Profile" />
        <activity android:name=".LearnActivity" />
        <activity android:name=".WhatToDo" />
        <activity android:name=".calcGame.GameOverActivity" />
        <activity
            android:name=".calcGame.GameWinActivity" />
    </application>

</manifest>